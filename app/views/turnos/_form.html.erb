<%= form_for @turno, :html => { :class => "form-horizontal turno" } do |f| %>

  <% if @turno.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@turno.errors.count, "error") %> prohibited this turno from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @turno.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :hora, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.time_select :hora, :class => 'form-control' %>
    </div>
    <%=f.error_span(:hora) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                turnos_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
