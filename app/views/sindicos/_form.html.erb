<%= form_for(@sindico) do |f| %>
  <% if @sindico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sindico.errors.count, "error") %> prohibited this sindico from being saved:</h2>

      <ul>
      <% @sindico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :telefone %><br>
    <%= f.number_field :telefone %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
